version: "3.9"

services:
  finance-mcp:
    build: .
    container_name: finance-mcp
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data       # база прямо в ./data проекта
    restart: unless-stopped
